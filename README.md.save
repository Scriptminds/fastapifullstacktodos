# FastAPI Task Manager

A clean and efficient task management API built with FastAPI.

## Features

- User Authentication
- CRUD operations for tasks
- JWT-based login
- SQLite database
- Simple modular codebase

## Tech Stack

- FastAPI
- SQLAlchemy
- SQLite
- OAuth2 + JWT

## How to Run

1. Clone the repo
2. Set up a virtual environment
3. Install dependencies
4. Run using Uvicorn

## Commands

```bash
git clone https://github.com/YOUR_USERNAME/fastapifullstacktodos.git
cd fastapifullstacktodos
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
uvicorn main:app --reload


---

### ✅ Step 2: **Save and Exit in Nano**

In nano, do this:

1. Press `Ctrl + O` → **This writes (saves) the file**
   - You’ll see: `File Name to Write: README.md`
   - Press `Enter` to confirm

2. Press `Ctrl + X` → **This exits nano**

---

### ✅ Step 3: Add & Push README to GitHub

Now back in the terminal:

```bash
git add README.md
git commit -m "📘 Add clean and professional README"
git push

pip freeze > requirements.txt
git add requirements.txt
git commit -m "📦 Add requirements.txt"
git push

